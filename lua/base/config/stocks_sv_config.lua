stocks = stocks or {}
stocks.server = stocks.server or {}
stocks.server.db = stocks.server.db or {}
stocks.server.errors = stocks.server.errors or {}
stocks.server.data = stocks.server.data or {}
stocks.server.stocks = stocks.server.stocks or {}
stocks.server.net = stocks.server.net or {}

stocks.server.stocks.Create("Coke", {
    cost = 500,
    min = 0,
    max = 1000,
    limit = 5,
})
stocks.server.stocks.Create("Amazon", {
    cost = 500,
    min = 0,
    max = 1000,
    limit = 1500,
})
stocks.server.stocks.Create("Pepsi", {
    cost = 500,
    min = 0,
    max = 1000,
    limit = 5,
})
stocks.server.stocks.Create("Tesla", {
    cost = 500,
    min = 0,
    max = 1000,
    limit = 5,
})
stocks.server.stocks.Create("Ford", {
    cost = 500,
    min = 0,
    max = 1000,
    limit = 5,
})
stocks.server.stocks.Create("Gamestop", {
    cost = 500,
    min = 0,
    max = 1000,
    limit = 5,
})
stocks.server.stocks.Create("Delorean", {
    cost = 500,
    min = 0,
    max = 1000,
    limit = 5,
})
stocks.server.stocks.Create("Space X", {
    cost = 500,
    min = 0,
    max = 1000,
    limit = 5,
})
stocks.server.stocks.Create("Microsoft", {
    cost = 500,
    min = 0,
    max = 1000,
    limit = 5,
})
stocks.server.stocks.Create("Turtle X", {
    cost = 500,
    min = 0,
    max = 1000,
    limit = 5,
})
stocks.server.stocks.Create("Google", {
    cost = 500,
    min = 0,
    max = 1000,
    limit = 5,
})
stocks.server.stocks.Create("Bing", {
    cost = 5,
    min = 0,
    max = 50,
    limit = 5,
})